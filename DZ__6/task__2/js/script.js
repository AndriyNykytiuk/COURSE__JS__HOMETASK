let columns 
let rows
let tree 

for (tree = 1; tree <= 3; tree++){
	
for (columns =7; columns >= 0; columns--){
	for (rows = 7; rows >= columns + 1; rows--) {
		document.write(`0`);
	}
	document.write(` <br>`);
	
		
}
}
